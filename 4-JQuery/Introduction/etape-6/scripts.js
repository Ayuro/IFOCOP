"use strict";
window.onload = function() {
    monOutil('.emplacement').unStyle('color', 'teal').texte('Le nouveau texte dans la page.');
    monOutil('#cible').unStyle('fontSize', '1.8rem').texte('Dernier changement de texte.');
};