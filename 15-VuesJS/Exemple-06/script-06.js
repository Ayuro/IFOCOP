const { createApp, ref, computed } = Vue;

const app = createApp({

    data() {
        return {
            title: '06 - VueJS - Utilisation plus élaborée de ref',
        };
    }
});

app.mount('#app')
