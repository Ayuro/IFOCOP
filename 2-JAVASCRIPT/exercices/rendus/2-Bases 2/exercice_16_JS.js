function ft_catch(){
    // Si l'utilisateur annule, return null
    // Si l'utilisateur envoi un champ vide, return true mais vide
    // Si l'utilisateur envoi un champ non vide, return envoi le prompt
    alert(prompt('Please tap your message'));
}